/* Essa linha garante que, assim que a página terminar de carregar o HTML, 
sua função carregarDados() será chamada automaticamente para preencher a tabela. */

window.addEventListener('DOMContentLoaded', carregarDados);
